/**
 * Polymorphism is the ability to use a common interface for multiple forms (data types).
 * Check the typescript/abstraction/abstraction-1.ts example.
 * 
 * The implementation of the Shape when creating the class Circle and Rectangle,
 * can be considered as an example of polymorphism.
 */